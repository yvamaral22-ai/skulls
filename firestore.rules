rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGRA DE PRODUÇÃO: Uso exclusivo do barbeiro mestre.
    // Permite leitura e escrita total para que os dados sejam salvos sem restrições.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}